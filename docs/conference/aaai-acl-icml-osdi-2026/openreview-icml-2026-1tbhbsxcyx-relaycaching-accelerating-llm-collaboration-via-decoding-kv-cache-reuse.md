---
title: "RelayCaching: Accelerating LLM Collaboration via Decoding KV Cache Reuse"
title_zh: RelayCaching：通过解码KV缓存复用加速LLM协作
authors: "Yingsheng Geng, Yuchong Gao, Weihong Wu, Guyue Liu, Jiang liu"
date: 2026-04-30
pdf: "https://openreview.net/pdf/dd7715f9e938a8a2b1ec56452c5e47284e3f86fe.pdf"
tags: ["query:agent-cache"]
score: 10.0
evidence: 直接复用上一智能体解码阶段的KV缓存到后续智能体预填充阶段，实现多智能体跨上下文缓存复用
tldr: 多智能体LLM协作中，前序智能体生成的共享内容会造成冗余预fill计算和KV缓存开销。RelayCaching提出无需训练的推理方法，直接复用前一智能体解码阶段的KV缓存进到后续预fill阶段，避免重新计算共享前缀。该方法在保持准确率的同时提高缓存复用率，显著降低显存使用和首token延迟，为智能体流水线中的跨上下文KV缓存共享提供了高效且准确的解决方案。
source: ICML-2026-Accepted
selection_source: conference_retrieval
motivation: 多智能体协作中存在大量冗余预fill计算，导致KV缓存内存和TTFT显著增加。
method: 提出RelayCaching，训练无关地将前序智能体解码阶段的KV缓存复用到后续预fill阶段，以去除共享内容重复计算。
result: 相比现有KV缓存方法，在保持准确率的前提下获得更高缓存复用率，并降低内存和时延。
conclusion: 通过解码阶段到预fill阶段的KV缓存直接复用，可有效加速多智能体LLM协作并减少资源浪费。
---

## Abstract
The increasing complexity of AI tasks has shifted the paradigm from monolithic models toward multi-agent large language model (LLM) systems. However, these collaborative architectures introduce a critical bottleneck: redundant prefill computation for shared content generated by previous agents, which significantly increases KV cache memory usage and time-to-first-token (TTFT). While various KV cache methods have been proposed to mitigate prefill redundancy, they either fail to maintain accuracy on agent-generated outputs or exhibit low reuse rates due to rigid constraints. We present RelayCaching, a training-free inference method that directly reuses decoding phase KV caches from previous agents in subsequent prefill phases. Our key insight is that KV caches for identical content are highly consistent across phases, while prefix-induced deviations are sparse and localized within a limited range of layers and token positions. By selectively recomputing KV caches at these positions, RelayCaching preserves model accuracy with minimal overhead, yielding a superior accuracy–efficiency trade-off over existing methods. Experiments on diverse collaborative LLM tasks spanning mathematical reasoning, general knowledge, and code generation demonstrate that RelayCaching achieves over $80$\% KV cache reuse, reduces TTFT by up to $4.7\times$ compared to the standard pipeline, all with negligible accuracy degradation.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机与背景）

- **背景**：随着 AI 任务复杂度提升，LLM 系统从单体模型向多智能体（multi-agent）协作架构演进。多个 LLM 智能体在流水线中协作，后序智能体往往需要处理前序智能体已生成的内容。
- **核心问题**：这种协作架构引入了关键瓶颈——对前序智能体生成的共享内容，后序智能体在预填充（prefill）阶段会进行冗余计算。这导致：
  - KV 缓存内存占用显著增加；
  - 首 token 延迟（TTFT）显著上升；
- **现有方法的不足**：已有的 KV 缓存复用方法要么无法在智能体生成内容上保持准确率，要么因约束过于僵化而导致缓存复用率低。
- **论文目标**：在不牺牲准确率的前提下，提高多智能体协作中 KV 缓存的复用率，减少冗余计算、降低内存和延迟。

## 2. 方法论

- **核心思想**：RelayCaching 是一种**无需训练**的推理方法，直接**将前序智能体解码（decoding）阶段的 KV 缓存复用到后续智能体的预填充（prefill）阶段**，从而避免对共享前缀的重新计算。
- **关键洞察**：
  - 相同内容在不同阶段产生的 KV 缓存具有高度一致性；
  - 前缀差异引起的偏差是**稀疏**的，且**局限在有限的层和 token 位置范围内**。
- **技术细节**：
  - 无需修改模型参数，属于训练无关（training-free）方法；
  - 通过选择性重算（selectively recompute）特定位置的 KV 缓存来恢复准确率；
  - 选择性重算只覆盖偏差存在的少量层和 token 位置，因此额外开销很小。
- **算法流程（文字描述）**：
  1. 前序智能体完成解码，得到完整 KV 缓存；
  2. 将解码阶段的 KV 缓存直接传给后续智能体作为预填充阶段的初始缓存；
  3. 检测或识别因前缀差异而偏离的层和 token 位置；
  4. 仅对这些位置的 KV 缓存进行选择性重新计算；
  5. 后续智能体基于修复后的 KV 缓存继续生成，无需全量重新预填充。
- 论文中未提供完整数学公式，但通过“稀疏偏差 + 选择性重算”框架描述了方法的核心机制。

## 3. 实验设计

- **任务场景**：
  - 数学推理（mathematical reasoning）
  - 通用知识问答（general knowledge）
  - 代码生成（code generation）
- **基准**：使用了多样化协作 LLM 任务，具体数据集名称在摘要中未列出（需要查阅原文）。
- **对比方法**：摘要提到“existing KV cache methods”，但未具体列出方法名称；对比指标包括缓存复用率、TTFT、准确率等。
- **核心实验结果**：
  - KV 缓存复用率超过 **80%**；
  - 相比标准流水线（standard pipeline），TTFT 最高降低 **4.7×**；
  - 准确率下降可忽略不计（negligible accuracy degradation）。

## 4. 资源与算力

- **摘要和元数据中未明确说明**使用的 GPU 型号、数量、训练时长等算力资源。
- 由于该方法属于训练无关推理方法，所需算力主要体现在推理阶段的 KV 缓存传输与选择性重算上，但具体硬件配置、模型规模（如参数数量）均未在摘要中提供。
- 需要查看论文全文以获取实验环境细节。

## 5. 实验数量与充分性

- **实验覆盖**：涵盖三个任务领域（数学推理、通用知识、代码生成），属于多场景评估，但摘要未披露每个领域的具体数据集数量和样例规模。
- **对比实验**：与现有 KV 缓存方法进行了对比，但具体基线数量不明。
- **消融实验**：摘要未明确提及是否包含消融实验（如选择性重算的层数/位置敏感性分析），需原文确认。
- **充分性与公平性**：
  - 从摘要看，实验覆盖了不同任务类型，结果显著，但缺乏具体实验数量、误差棒、统计显著性等信息；
  - 对比方法未具名，公平性难以从摘要单独判断；
  - 选择性重算的“稀疏偏差”假设是否对更多边界场景（如长上下文、复杂多轮交互）成立，需要更多测试。

## 6. 主要结论与发现

- RelayCaching 通过直接复用前序智能体解码阶段的 KV 缓存，能有效消除多智能体流水线中的冗余预填充计算。
- 相比现有 KV 缓存方法，RelayCaching 在保持准确率的同时，实现了更高的缓存复用率，并显著降低了 KV 缓存内存占用和 TTFT。
- 该方法为跨上下文 KV 缓存共享提供了高效且准确的解决方案，尤其适用于智能体流水线场景。

## 7. 优点

- **创新性**：提出从“解码阶段”到“预填充阶段”的 KV 缓存直接复用路线，不同于常见仅在同一上下文内部复用缓存的思路。
- **训练无关**：无需额外训练或微调，易于部署到现有多智能体推理系统中。
- **高效性**：仅需重算少量稀疏偏差位置，避免全量重新预填充，开销小。
- **准确率保持**：针对前缀偏差进行选择性修正，在加速的同时避免了准确率明显下降。
- **性能提升显著**：缓存复用率超 80%，TTFT 最高降低近 5 倍，展示出较强的实用价值。

## 8. 不足与局限

- **信息不完整**：摘要和元数据未提供实验环境的算力细节、数据集名称、模型尺寸、基线方法列表，难以完全评估可复现性和泛化性。
- **假设依赖**：方法依赖“KV 缓存一致性高、偏差稀疏”这一假设，如果智能体输出内容差异大、上下文长度极长或任务交互复杂，偏差可能不再稀疏，选择性重算的成本可能上升，效果可能受限。
- **实验充分性存疑**：未在摘要中展示消融研究、不同代理数量/流水线深度下的扩展性、以及与其他高性能缓存方法的逐项对比结果。
- **应用限制**：仅适用于多智能体之间的共享内容复用，对没有共享前缀或共享比例低的场景收益有限；需要额外的缓存传输/存储管理，可能引入系统复杂性。
- **偏差检测机制未明确**：如何准确识别需要重算的层和 token 位置，摘要未给出具体算法或阈值，可能是一个实现难点。

（完）
