---
title: "RelayCaching: Accelerating LLM Collaboration via Decoding KV Cache Reuse"
title_zh: 中继缓存：通过解码KV缓存复用加速LLM协作
authors: "Yingsheng Geng, Yuchong Gao, Weihong Wu, Guyue Liu, Jiang liu"
date: 2026-04-30
pdf: "https://openreview.net/pdf/dd7715f9e938a8a2b1ec56452c5e47284e3f86fe.pdf"
tags: ["query:cache-reuse"]
score: 10.0
evidence: 直接在多智能体LLM系统中跨智能体复用解码阶段的KV缓存，避免冗余预填充，符合跨上下文KV缓存复用需求。
tldr: 多智能体大模型协作中，不同智能体经常处理共享内容，导致预填充阶段发生大量冗余计算，KV缓存占用与首令牌时延显著上升。现有KV缓存复用方法在智能体生成内容上难以兼顾准确性和高复用率。本文提出免训练的RelayCaching方法，直接复用前一智能体的解码KV缓存到后续预填充阶段，从而显著减少冗余预填充，并在保持准确性的同时提升缓存复用率，为多智能体协作推理提供了有效的缓存加速机制。
source: ICML-2026-Accepted
selection_source: conference_retrieval
motivation: 多智能体LLM协作中共享内容的冗余预填充增加了KV缓存内存和首令牌时延。
method: 提出RelayCaching，训练无关地将上一智能体的解码KV缓存复用到下一智能体的预填充阶段。
result: 在保持准确率的同时显著降低KV缓存占用与首令牌时延，复用率和效果均优于现有方法。
conclusion: 说明跨智能体解码KV缓存复用可有效加速LLM协作推理。
---

## Abstract
The increasing complexity of AI tasks has shifted the paradigm from monolithic models toward multi-agent large language model (LLM) systems. However, these collaborative architectures introduce a critical bottleneck: redundant prefill computation for shared content generated by previous agents, which significantly increases KV cache memory usage and time-to-first-token (TTFT). While various KV cache methods have been proposed to mitigate prefill redundancy, they either fail to maintain accuracy on agent-generated outputs or exhibit low reuse rates due to rigid constraints. We present RelayCaching, a training-free inference method that directly reuses decoding phase KV caches from previous agents in subsequent prefill phases. Our key insight is that KV caches for identical content are highly consistent across phases, while prefix-induced deviations are sparse and localized within a limited range of layers and token positions. By selectively recomputing KV caches at these positions, RelayCaching preserves model accuracy with minimal overhead, yielding a superior accuracy–efficiency trade-off over existing methods. Experiments on diverse collaborative LLM tasks spanning mathematical reasoning, general knowledge, and code generation demonstrate that RelayCaching achieves over $80$\% KV cache reuse, reduces TTFT by up to $4.7\times$ compared to the standard pipeline, all with negligible accuracy degradation.

---

## 论文详细总结（自动生成）

根据提供的论文信息，以下是对该论文的详细中文总结。

## 1. 核心问题与整体含义

- **研究动机**：随着AI任务复杂性增加，多智能体大语言模型（LLM）系统逐渐取代单体模型成为主流架构。在这种协作架构中，存在一个关键瓶颈：当前序智能体生成共享内容后，后续智能体需要对相同内容进行**冗余的预填充（prefill）计算**。这种冗余计算不仅显著增加了KV缓存的内存占用，还大幅提升了首token延迟（TTFT）。
- **整体含义**：论文聚焦于多智能体LLM协作推理中的缓存复用问题。现有KV缓存复用方法存在两难困境：要么无法在智能体生成的内容上保持准确性，要么因为约束过于严格而导致复用率低下。论文旨在解决这一准确性与复用率之间的冲突，为多智能体推理提供更高效的缓存复用方案。

## 2. 方法论：RelayCaching

- **核心思想**：论文提出RelayCaching，一种**免训练（training-free）**的推理方法。其关键洞见在于：
  - 对于相同内容，不同阶段（解码阶段与预填充阶段）生成的KV缓存具有高度一致性。
  - 由前缀（prefix）导致的偏差是稀疏的，并且仅局限于特定层和token位置的有限范围内。
- **技术细节与算法流程**：
  1. 将前序智能体在**解码阶段**生成的KV缓存直接复用到后续智能体的**预填充阶段**，避免对共享内容进行重复计算。
  2. 为了保持模型精度，方法首先识别出因前缀差异而导致KV缓存发生偏差的稀疏位置（限定在一定的层数和token位置范围内）。
  3. 仅在这些偏差位置**选择性重新计算**KV缓存，从而以最小的额外开销修正偏差。
  4. 通过这种“识别偏差 + 局部重算”的策略，RelayCaching在消除冗余预填充计算的同时，保持了模型的输出准确性。
- **公式说明**：论文提供的材料中未包含具体的数学公式，其算法流程主要基于上述“对比→识别偏差区位→选择重算”的朴素逻辑展开。

## 3. 实验设计

- **评测场景与Benchmark**：论文在多种协作式LLM任务上进行了实验，涵盖了：
  - **数学推理（mathematical reasoning）**
  - **通用知识（general knowledge）**
  - **代码生成（code generation）**
- **对比方法**：与**标准管线（standard pipeline）**进行了对比，并涉及与其他现有KV缓存复用方法的比较（论文提到其他方法在精度或复用率上存在不足，RelayCaching与之对比体现了更优的精度-效率权衡）。
- **关键指标**：包括KV缓存复用率、TTFT（首token延迟）、输出准确性等。

## 4. 资源与算力

- **未明确说明**：提供的论文材料（摘要及元数据）中**未提及**所使用的GPU型号、数量或训练时长等具体算力资源信息。
- **说明**：由于该方法是免训练的推理优化方法，推测其核心实验成本聚焦于推理阶段的端侧运行开销而非训练开销，但具体硬件配置未披露。

## 5. 实验数量与充分性

- **实验数量**：论文在3类不同的任务场景（数学、知识、代码）上进行了验证，并给出了明确的量化效果指标。
- **充分性与客观性分析**：
  - **充分性**：覆盖了3类典型需要深度推理或知识检索的协作任务，具备一定的广度。但材料中未提及是否进行了专门的消融实验（例如验证偏差位置识别机制的有效性、不同层数/位置范围对精度的影响等）。
  - **客观公平性**：指标上同时关注了效率（TTFT、复用率）与准确性，避免了只看速度而忽视质量的偏差。但由于缺少与其他前沿缓存复用方法的具体数值对比细节，难以完全评估其对比的详尽程度。

## 6. 主要结论与发现

- **高效复用**：RelayCaching实现了**超过80%的KV缓存复用率**。
- **显著加速**：与标准管线相比，将TTFT降低了**高达4.7倍**。
- **精度无损**：在实现上述效率提升的同时，仅存在**可忽略不计的精度下降（negligible accuracy degradation）**。
- **最终结论**：该方法为多智能体LLM协作中的缓存复用提供了一种简单高效的训练无关方案，有效缓解了协作推理中预填充冗余带来的瓶颈问题。

## 7. 优点

- **创新性强**：提出“复用解码阶段缓存到预填充阶段”的新思路，打破传统预填充-解码的固化流程，切入点巧妙。
- **实用性强**：**免训练**的特点使其无需额外微调或改动模型权重，即插即用，便于在现有系统中快速部署。
- **精度与效率兼顾**：巧妙利用偏差稀疏性进行选择性重算，以极小代价换取高精度，实现了优于现有方法的“精度-效率”权衡。

## 8. 不足与局限

- **信息缺失**：论文材料中未提供具体的算力资源、基线方法的详细配置以及消融实验细节，使得实验的复现性和全面性评估受限。
- **偏差机制假设**：方法的有效性高度依赖于“前缀引起的偏差是稀疏且局域化”的假设。若在超长上下文或特定复杂推理场景中偏差范围显著扩大，其局部重算的代价可能增加。
- **普适性验证**：虽然覆盖了3类任务，但多智能体协作场景多种多样，是否在所有协作模式下（如深度级联、横向协作等）均能保持80%以上的复用率尚需更多验证。

（完）
