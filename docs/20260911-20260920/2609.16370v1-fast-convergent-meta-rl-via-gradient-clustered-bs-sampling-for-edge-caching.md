---
title: Fast-Convergent Meta-RL via Gradient-Clustered BS Sampling for Edge Caching
title_zh: 面向边缘缓存的梯度聚类基站采样快速收敛元强化学习
authors: "Farnaz Niknia, Ping Wang"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.16370v1"
tags: ["query:mas-cache"]
score: 6.0
evidence: 多基站缓存智能体共享元学习初始化
tldr: 无线边缘缓存中大量基站结构相同却需各自从零训练RL，元强化学习可共享初始化，但大规模元训练时随机采样基站导致元梯度高方差。本文提出梯度聚类基站采样：按局部梯度相似度分组，并按簇大小比例抽取基站，配合各基站PPO/SMDP与MAML式元循环。理论证明该采样器在基站异质下元梯度方差严格低于均匀随机采样，从而加速元训练收敛。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.16370v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 902, \"height\": 625, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.16370v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1306, \"height\": 703, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.16370v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 906, \"height\": 698, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.16370v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 909, \"height\": 699, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.16370v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 874, \"height\": 1756, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.16370v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 794, \"height\": 742, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.16370v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1052, \"height\": 243, \"label\": \"Table\"}]"
motivation: 边缘缓存基站独立且异构，逐站从零训练RL代价高；元RL虽可共享初始化，但大规模元训练中均匀随机采样基站使元梯度估计高方差，成为收敛瓶颈。
method: 基站用PPO在SMDP中决策内容流行度、大小、寿命与重要性；共享MAML式元策略，按局部梯度相似度聚类基站，每轮按簇大小比例采样估计元梯度。
result: 基于ANOVA式梯度方差分解，证明在基站异质下，梯度聚类采样得到的元梯度估计方差严格低于均匀随机采样，并可加快元训练收敛。
conclusion: 该框架缓解大规模元RL缓存训练瓶颈，为异构基站提供低方差元梯度采样与快速适应方案，推动可扩展元缓存部署。
---

## 摘要
无线边缘缓存网络通常由许多独立基站（BS）组成，每个基站面临各自的请求速率和内容流行度分布。在每个基站上从零开始训练一个强化学习（RL）缓存智能体，会迫使每个智能体通过缓慢的试错重新学习一个在整个网络中结构相同的决策问题。元强化学习通过学习一个共享初始化来消除这种冗余，该初始化可在少量本地更新后适应任意基站；然而，元训练本身在大规模下成为瓶颈：在每次元迭代中，元梯度必须从一小部分基站上估计，而均匀随机采样这一子集会产生高方差估计，现有元强化学习缓存框架未解决该问题。本文提出一个面向独立、非重叠基站间缓存的元强化学习框架，直接针对这一瓶颈。每个基站运行一个本地近端策略优化（PPO）智能体，该智能体被建模为关于内容流行度、大小、生命周期和重要性的半马尔可夫决策过程（SMDP），同时通过模型无关元学习（MAML）风格的循环学习共享元策略。为了扩展元训练并加速收敛，我们引入基于梯度的聚类，它根据本地梯度相似性对基站分组，并在每次元迭代中按簇大小比例从每个簇中抽取样本。我们通过梯度方差的方差分析（ANOVA）式分解证明，在基站异质性下，该策略产生的元梯度估计器比均匀随机采样具有严格更低的方差。

## Abstract
Wireless edge caching networks typically consist of many independent Base Stations (BSs), each facing its own request rate and content popularity profile. Training a Reinforcement Learning (RL) caching agent from scratch at every BS forces each agent to relearn, through slow trial and error, a decision problem that is structurally identical across the network. Meta-reinforcement learning removes this redundancy by learning a shared initialization that adapts to any BS in a few local updates; however, meta-training itself becomes the bottleneck at scale: the meta-gradient must be estimated from a small subset of BSs at each meta-iteration, and sampling this subset uniformly at random yields a high-variance estimate, an issue existing meta-RL caching frameworks leave unaddressed. This paper proposes a meta-reinforcement learning framework for caching across independent, non-overlapping BSs that directly targets this bottleneck. Each BS runs a local Proximal Policy Optimization (PPO) agent, formulated as a Semi-Markov Decision Process (SMDP) over content popularity, size, lifetime, and importance, while a shared meta-policy is learned via a Model-Agnostic Meta-Learning (MAML)-style loop. To scale meta-training and accelerate convergence, we introduce gradient-based clustering, which groups BSs by local gradient similarity and draws from every cluster, in proportion to its size, at each meta-iteration. We prove, via an Analysis of Variance (ANOVA)-style decomposition of gradient variance, that this strategy yields a strictly lower-variance meta-gradient estimator than uniform random sampling under BS heterogeneity.