---
title: "Agent-UCT: Upper Confidence Bounds Applied to Trees for Agentic Workflow Optimization with Cost-Awareness"
title_zh: Agent-UCT：具有成本意识的代理工作流优化树搜索上置信界算法
authors: "Yang Li, Hai Liu, Dian Shao, Yu Wang, Xiyu Chen, Sergey Volkov, Bozhi Wang, Ziyu Sun, Sihang Liu, Ye Luo, Xiaowei Zhang"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24162v2"
tags: ["query:agent-cache"]
score: 9.0
evidence: 通过二分前缀复用图实现复用感知正则化，利用代理工作流中已物化的计算
tldr: "代理工作流（如RAG管道）自动优化需在离散组件组合空间搜索，评估成本高昂。Agent-UCT算法扩展UCF应用于树搜索，引入基于二分图前缀重用图的复用感知正则化，偏向选择可重用已物化配置前缀的分支，消除冗余计算。统一框架RAGSpace将异质RAG组件重组为五维空间，WTB通过确定性重放和缓存确保一致性。在HotpotQA和UltraDomain上，找到的配置样本外性能最佳，全量评估下逻辑搜索代价降低73.6%，采样评估额外加速4.2倍，显著降低冗余计算，为成本感知的工作流优化提供新范式。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24162v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 870, \"height\": 412, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24162v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 686, \"height\": 731, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24162v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 841, \"height\": 440, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24162v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 864, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24162v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 848, \"height\": 304, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24162v2/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 860, \"height\": 300, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24162v2/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 799, \"height\": 362, \"label\": \"Table\"}]"
motivation: 现有方法忽视代理工作流组合的结构化复用，导致大量冗余计算，亟需成本感知的搜索优化。
method: Agent-UCT在UCT树搜索中引入二分图前缀重用正则化，激励复用已物化配置；RAGSpace统一五维RAG组件空间，WTB提供确定性缓存与重放。
result: "在HotpotQA与UltraDomain上，找到的配置优于固定框架预设，全量评估下前缀重用降低73.6%逻辑搜索代价，采样评估再加速4.2倍。"
conclusion: Agent-UCT通过前缀复用与统一框架，显著降低代理工作流优化成本，为可重现的自动工作流设计提供新思路。
---

## 摘要
优化代理工作流，如检索增强生成（RAG）流水线，需要在紧张的评估预算下导航离散组件选择的组合空间。现有方法——启发式搜索、黑盒优化和标准树搜索方法——并未明确利用这些工作流的组合结构，导致冗余计算和预算分配低效。我们提出Agent-UCT（基于代理的成本感知树搜索上置信界算法），一种扩展UCT的树搜索算法，其加入了一个来自二分前缀重用图的重用感知正则项。Agent-UCT倾向于选择利用先前物化的配置前缀的分支，减少冗余执行同时保持有效探索。我们的框架RAGSpace将来自LongRAG、LightRAG和Self-RAG的异构RAG组件统一到一个五维配置空间中，实现跨框架的系统性重组。WTB（工作流测试台）提供确定性重放、内容寻址缓存和事务一致性，确保中间状态物化一次并在整个搜索过程中重用。在HotpotQA和UltraDomain上的实验表明，Agent-UCT识别的配置在评估的固定框架预设中具有最高的样本外性能。在全池评估下，二分前缀重用相对于无前缀共享成本上限减少了73.6%的逻辑搜索成本。与全池评估相比，基于采样的评估进一步实现了4.2倍的墙钟加速。Agent-UCT、RAGSpace和WTB共同提供了一个统一的框架，用于成本感知、可重现且组合高效的代理工作流优化。

## Abstract
Optimizing agentic workflows, such as retrieval-augmented generation (RAG) pipelines, requires navigating a combinatorial space of discrete component choices under tight evaluation budgets. Existing approaches - heuristic search, black-box optimization, and standard tree search methods - do not explicitly exploit the compositional structure of these workflows, leading to redundant computation and inefficient budget allocation. We introduce Agent-UCT (Agent-based Cost-Aware Upper Confidence Bounds Applied to Trees), a tree search algorithm that extends UCT with a reuse-aware regularization term derived from a bipartite prefix reuse graph. Agent-UCT biases selection toward branches that leverage previously materialized configuration prefixes, reducing redundant execution while maintaining effective exploration. Our framework, RAGSpace, unifies heterogeneous RAG components from LongRAG, LightRAG, and Self-RAG into a five-dimensional configuration space, enabling systematic cross-framework recombination. WTB (Workflow Test Bench) provides deterministic replay, content-addressable caching, and transactional consistency, ensuring that intermediate states are materialized once and reused across the search. Experiments on HotpotQA and UltraDomain demonstrate that Agent-UCT identifies configurations with the highest out-of-sample performance among the evaluated fixed framework presets. Under full-pool evaluation, bipartite prefix reuse reduces logical search cost by 73.6% relative to the no-prefix-sharing cost upper bound. Compared with full-pool evaluation, sampling-based evaluation further achieves a 4.2x wall-clock speedup. Agent-UCT, RAGSpace, and WTB together provide a unified framework for cost-aware, reproducible, and compositionally efficient agentic workflow optimization.

---

## 论文详细总结（自动生成）

好的，以下是对论文《Agent-UCT: Upper Confidence Bounds Applied to Trees for Agentic Workflow Optimization with Cost-Awareness》的结构化、深入、客观的总结。

### 1. 论文的核心问题与整体含义

- **核心问题**：如何高效地优化由离散组件构成的代理工作流（如RAG流水线）。这类优化是一个组合爆炸问题，且每次完整的配置评估都涉及昂贵的LLM调用和检索操作，成本极高。
- **现有缺陷**：现有方法（如启发式搜索、贝叶斯优化、标准蒙特卡洛树搜索）未能有效利用工作流的组合结构。它们将工作流视为黑盒，忽略了“不同配置可能共享相同的前缀计算”这一特性，导致大量冗余计算和评估预算的低效分配。
- **整体含义**：该论文旨在提出一种成本感知的优化框架，通过智能地探索配置空间并重用已物化的中间状态，在有限的评估预算下找到性能优异的代理工作流配置，从而提升优化效率和可复现性。

### 2. 论文提出的方法论

论文的核心贡献包括一个算法（Agent-UCT）和两个支撑系统（RAGSpace， WTB）。

- **核心思想**：
    - 将工作流优化建模为树搜索问题，其中节点代表部分配置（前缀）。
    - 在搜索过程中，通过在标准UCT算法的选择策略中加入一个“重用感知正则化项”，偏向于选择那些可以利用先前已执行（物化）的中间状态的分支，从而避免重复计算。

- **关键技术细节**：
    - **Agent-UCT算法**：
        - **选择策略**：修改了标准UCT的得分函数，从 `UCT得分 = 平均奖励 + 探索项` 扩展为 `Agent-UCT得分 = 平均奖励 + 探索项 - 正则化系数 × 边际计算成本`。
        - **边际成本（ΔC）**：通过一个二分前缀重用图来估算。该图记录了哪些配置前缀在哪些基准测试集（或问题）上已经物化，从而可以精确计算展开到某个新子节点时，有多少计算可以省去。
        - **衰减正则化**：正则化系数 \( \lambda_t = \lambda_0 / \sqrt{t} \) 随时间衰减，使得搜索初期，算法更偏好重用成本低的分支。随着奖励估计变得更可靠，成本的影响逐渐减弱。
    - **双层级二分重用图**：
        - **集群级图**：用于全量评估模式，记录前缀在基准测试集集群上的物化状态。
        - **题目级图**：用于采样评估模式，精细地记录前缀在单个题目上的物化状态，以支持更细粒度的重用和成本估算。
    - **支撑框架**：
        - **RAGSpace**：将来自LongRAG、LightRAG和Self-RAG的异构组件标准化到一个统一的五维配置空间（分块、查询、检索、后检索、生成），实现了跨框架的组件重组。
        - **WTB（工作流测试台）**：基于AgentGit的确定性执行、内容寻址缓存和事务一致性保障，是Agent-UCT算法能够可靠地实现状态复用的执行基础。
    - **算法流程（采样模式）**：
        1. 从根节点开始，为非终结且完全展开的节点，使用Agent-UCT得分选择最优动作，直至到达叶节点。
        2. 到达叶节点后，随机选择一个未尝试的动作进行扩展。
        3. 将新节点补全为完整配置，在采样的问题批次上进行评估，得到奖励估计。
        4. 更新题目级二分重用图，并将奖励沿树路径反向传播，更新各节点的统计信息。

### 3. 实验设计

- **数据集与场景**：
    - **HotpotQA**：多跳问答基准，按困难度（easy, medium, hard）划分为三个集群，使用精确匹配（EM）和F1分数评估。
    - **UltraDomain**：领域特定的长文问答基准，覆盖农业、计算机科学、法律和混合四个领域，使用token级F1评估。
    - 数据集均被划分为搜索集（350条，用于Agent-UCT搜索）和测试集（150条，用于样本外评估）。
- **对比方法（Baseline）**：
    - 固定框架的预设配置：**LongRAG**, **LightRAG**, **Self-RAG**。
- **搜索空间**：
    - Agent-UCT在由RAGSpace定义的384种可能的跨框架组件组合（笛卡尔积）中进行搜索。所有对比基线都只在其自身的固定预设内。

### 4. 资源与算力

- **计算资源**：论文未明确提及所使用的GPU型号、数量或显存。
- **模型与成本**：
    - **骨干模型**：所有实验均使用 `openai/gpt-5.4-nano` 模型。
    - **运行时长**：全量评估模式耗时**157.6小时**；采样评估模式耗时**37.9小时**，实现了**4.2倍**的墙钟加速。
    - **API成本**：采样模式相比全量模式，总LLM token消耗减少至六分之一以下（633.1M vs 110.4M），Chat API调用次数减少至约八分之一（228,487次 vs 29,056次）。

### 5. 实验数量与充分性

- **实验组数**：实验设计较为充分，主要包括以下组对比：
    1.  **目标特定搜索实验**：分别以HotpotQA-only， UltraDomain-only，以及HQ+UD联合作为优化目标进行搜索，并评估找到的最佳配置。
    2.  **样本外泛化性实验**：将上述搜索找到的配置在独立测试集上评估，与三个固定框架基线进行对比。
    3.  **成本效率分析**：对比全量评估与采样评估在物化键数量、缓存命中率等指标上的差异。
    4.  **消融研究**：通过计算实际搜索成本与无前缀共享的理论成本上限，量化了二分前缀复用的效率提升（73.6%）。
    5.  **组件归因分析**：通过ANOVA和随机森林两种方法，分析了pipeline中各组件对最终奖励方差的贡献度。
- **充分性与客观性**：
    - **充分性**：实验覆盖了多目标、多数据集，并系统地检验了性能、泛化能力和效率三大维度，内容全面。
    - **客观公平性**：对比是在统一的RAGSpace空间和WTB执行后端上进行，保证了对比的公平性。使用了统计显著性检验（置换检验）来验证性能提升是否显著。

### 6. 论文的主要结论与发现

1.  **性能更优**：Agent-UCT搜索发现的配置，在HotpotQA和UltraDomain的样本外测试中，其性能显著优于LightRAG和SelfRAG固定预设，并与LongRAG相当或更优。在联合目标下，Agent-UCT找到的配置取得了最高的综合F1分数。
2.  **效率极高**：
    - **逻辑成本**：相比于无重用的理论成本上限，二分前缀复用使逻辑搜索成本**降低了73.6%**。
    - **计算成本**：采样评估模式相比全量评估，实现了**4.2倍的墙钟加速**，并大幅减少了token消耗和API调用次数。
3.  **组件重要性**：在评估的RAG流水线中，**生成（generation）** 和**检索（retrieval）** 两个组件对最终性能贡献最大，这一发现在不同评估模式下具有一致性。

### 7. 优点

- **创新性的成本感知机制**：通过“二分前缀重用图”将路径依赖的计算复用机会显式地集成到树搜索的选择策略中，从算法层面解决了冗余计算问题，这是区别于其他方法的核心亮点。
- **系统与算法的协同设计**：Agent-UCT（算法）与RAGSpace（统一空间）和WTB（可重用执行后端）紧密结合，形成了一个完整、可操作且可复现的优化框架，实用性很强。
- **灵活的评估与成本估计**：提出的集群级和题目级双层级重用图，巧妙地支撑了全量和采样两种评估模式下的成本估算，兼顾了评估精度和效率。
- **实验验证全面**：同时关注了性能提升、泛化能力和效率收益，并进行了统计检验和重要性归因分析，论证严谨。

### 8. 不足与局限

- **搜索空间受限**：配置空间仅由LongRAG、LightRAG和Self-RAG的组件构成，未能穷尽更广泛的RAG组件或工作流结构设计空间。
- **单一骨干模型**：所有实验仅基于一个特定LLM（`gpt-5.4-nano`）完成，结论对不同模型能力或规模的可扩展性未经验证。
- **数据集规模限制**：出于成本考虑，使用的是HotpotQA和UltraDomain的分层抽样子集，而非完整基准，可能影响结论的代表性。
- **评估指标的局限性**：对UltraDomain这类需要长文回答的数据集，自动评价使用的token级F1可能无法完全捕捉回答的事实准确性和完整性。
- **系统能力的未充分探索**：论文提到WTB支持更复杂文件系统级别的重用和缓存，但目前的实验仅用了题目和工作流级别的特性，更深层次的重用潜力尚待挖掘。
- **硬件细节缺失**：论文未提供GPU型号、数量等硬件配置细节，不利于精确复现其墙钟时间结果。

（完）
