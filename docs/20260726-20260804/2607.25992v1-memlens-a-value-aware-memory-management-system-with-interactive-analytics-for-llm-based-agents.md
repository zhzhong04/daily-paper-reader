---
title: "MemLens: A Value-Aware Memory Management System with Interactive Analytics for LLM-based Agents"
title_zh: MemLens：一种面向基于大语言模型的智能体的价值感知内存管理系统，具备交互式分析功能
authors: "Shuyue Wei, Chang Liu, Zimu Zhou, Yongxin Tong, Lizhen Cui"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25992v1"
tags: ["query:agent-cache"]
score: 8.0
evidence: 面向LLM代理的价值感知内存管理，通过Shapley式评估实现驱逐
tldr: 鉴于现有LLM智能体记忆系统粗粒度处理交互数据，忽视价值差异，导致低效与冗余。MemLens系统引入价值感知机制，以Shapley值评估记录重要性，实现价值驱动存储。通过交互式分析面板，用户可直观监控记忆生命周期，并在实际应用中验证了其在响应质量、延迟和资源消耗上的优势。该系统为构建高效、可解释个性化长期记忆提供了解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25992v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 756, \"height\": 266, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25992v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 846, \"height\": 373, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25992v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 867, \"height\": 486, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25992v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 870, \"height\": 488, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25992v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 870, \"height\": 484, \"label\": \"Figure\"}]"
motivation: 现有LLM记忆系统粗粒度无差别处理记录，导致冗余和低效，影响长期推理与个性化。
method: 提出MemLens，以价值感知为核心，采用Shapley值评估记忆记录，并构建交互式分析面板覆盖记忆全生命周期。
result: 在study-copilot应用中，系统支持可视化记忆价值和层次结构，并比较多种策略在质量、延迟和token消耗上的表现。
conclusion: MemLens提供高效、可解释的个性化长期记忆管理，有望提升LLM智能体在复杂任务中的表现。
---

## 摘要
最近，内存管理已成为基于大语言模型的智能体的关键基础设施，因为它直接影响到长期推理、个性化响应和知识重用。然而，现有的LLM内存系统通常采用粗粒度（与效用无关）的方式，统一处理异构的用户-LLM交互记录，导致冗余和低影响的记录持续存在于内存存储库中。为了应对这一挑战，我们提出了MemLens，一种价值感知的内存管理系统，将内存记录视为一等数据对象。MemLens提供了一个端到端的交互式分析仪表板，展示了完整的内存生命周期，包括Shapley式的内存评估、价值感知的存储和内存辅助的响应。通过学习助手应用，系统使用户能够检查内存价值，可视化层次化内存结构，并比较各种内存管理策略在响应质量、检索延迟和令牌消耗方面的表现。因此，我们的MemLens可以作为一个高效、可解释且个性化的长期内存管理系统，用于基于大语言模型的智能体。

## Abstract
Recently, memory management has become a key infrastructure for LLM-based agents, as it directly affects long-horizon reasoning, personalized responses, and knowledge reuse. However, existing LLM memory systems typically adopt a coarse-grained (utility-agnostic) manner that treats heterogeneous user-LLM interaction records uniformly, leading to redundant and low-impact records persisting in the memory repository. To address this challenge, we present MemLens, a value-aware memory management system that takes memory records as first-class data objects. MemLens provides an end-to-end interactive analytics dashboard that exposes the complete memory lifecycle, including Shapley-style memory evaluation, value-aware storage, and memory-assisted response. Through a study-copilot application, the system enables users to inspect memory values, visualize hierarchical memory structures, and compare various memory management strategies in terms of response quality, retrieval latency, and token consumption. Therefore, our MemLens can serve as an efficient, interpretable, and personalized long-term memory management system for LLM-based agents.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机与背景）
- **核心问题**：现有基于大语言模型（LLM）的智能体记忆系统普遍采用“与效用无关”(utility-agnostic) 的粗粒度管理方式，无差别地存储所有用户-LLM 交互记录（如对话历史、推理痕迹、工具输出等）。这导致大量冗余、低价值的记忆片段堆积在记忆库中，稀释了与任务相关的上下文，扩大了检索空间，并浪费了不必要的 Token 开销。
- **整体含义**：论文旨在将记忆记录提升为“一等数据对象”，通过主动评估每一条记忆的**下游效用（价值）**，实现一种价值感知的记忆管理范式，从而在提升响应质量的同时大幅降低存储和推理成本。

### 2. 方法论：核心思想、关键技术细节与流程
论文提出 **MemLens** 系统，围绕“记忆的价值”构建了覆盖记忆**评估、存储、响应**三大环节的闭环生命周期，并提供交互式分析仪表板。

- **核心思想**：用经济学的 **Shapley 值** 思想量化每一条记忆单元对最终任务响应的边际贡献，并依据贡献值高低决定保留、组织和注入策略。
- **记忆数据评估**
    - **记忆单元构建**：将原始交互记录分割为句子级原子单元，再通过 LLM 聚合成多层次摘要节点，形成层次化记忆树（叶子为原子单元，内部节点为高层语义），并保留溯源链接。
    - **价值估计**：引入轻型代理模型 \(M_p\)（如 Qwen2.5-7B），定义一条记忆加入某个记忆子集 \(S\) 前后的输出质量差异作为效用函数 \(V_q(S)\)。利用 Shapley 值的定义计算每条记忆的贡献 \(\phi(m_i)\)。为降低计算复杂度（原本 \(O(2^M)\)），采用**分层抽样近似**，将复杂度降为 \(O(\rho M)\)（\(\rho\) 为采样预算，通常取 20~100）。
- **价值感知存储**
    - **选择性保留**：用户可设定一个价值阈值 \(\tau\)，仅保留 \(\phi(m_i) \ge \tau\) 的高价值记忆。
    - **层次化组织与合并**：新记忆进入系统时，通过 LLM 辅助的合并算子，依据语义相似度决定是合并到现有节点、更新节点还是作为新节点插入，以此动态维护记忆树并减少冗余。
- **记忆辅助响应**
    - **个性化注入**：从高价值历史记忆中提炼用户偏好画像作为系统提示，确保跨会话个性化的一致性。
    - **重排序检索**：从向量数据库中检索相关记忆，再依据 MS-value（记忆 Shapley 值）进行重排序，优先将“即语义相关又效用高”的记忆注入 LLM 上下文。
    - **闭环归档**：用户完成会话后可归档记录，触发新一轮评估与存储，形成持续演化的记忆生命周期。

### 3. 实验设计（数据集、场景、基准与对比方法）
- **演示场景**：通过一个**学习助手（study-copilot）** 应用进行交互式演示，帮助用户理解数据库核心概念（如事务、索引、并发控制）。
- **数据集**：构建了合成基准数据集 **EduMemBench**，包含：
    - 由 LLM 生成的学生智能体产生的 **2000+ 组多轮对话**。
    - 基于这些对话衍生出的 **500 对测试查询-答案对**（含参考答案），用于系统化评估。
- **对比方法**：在交互界面中支持并排比较以下三种记忆管理策略：
    1.  **全量存储 (store-all)**：保留所有记录。
    2.  **基于智能体的摘要 (agent-based summarization)**：使用摘要式启发策略。
    3.  **价值感知存储 (value-aware)**：MemLens 提出的策略。
- **评估指标**：在测试集上测量**响应质量、检索延迟和 Token 消耗**，并通过雷达图展示三者间的权衡。

### 4. 资源与算力
- 论文**未明确说明**进行实验或运行系统所需的 GPU 型号、数量或训练时长。
- 仅提及系统默认使用 **Qwen2.5-7B** 作为评估记忆价值时的轻型代理模型，并支持用户在仪表板中切换其他轻量模型。由于本工作重点在记忆管理框架与演示系统，所涉模型均为推理而非训练，故未披露具体算力开销。

### 5. 实验数量与充分性
- **实验类型**：主要提供了一组基于 EduMemBench 的**对比性演示实验**，用于可视化三种策略在响应质量、延迟和 Token 消耗上的差异。
- **充分性评估**：
    - **覆盖度不足**：实验局限于一个自建的、主题为数据库学习的合成数据集，缺乏在公开长期记忆基准（如 LoCoMo、LongMemEval）上的验证。尽管论文声称 MemLens 不依赖特定基准，但并未提供跨领域、跨场景的实验结果。
    - **消融与深度有限**：未展示针对价值阈值 \(\tau\)、代理模型选择、Shapley 采样预算 \(\rho\) 等关键参数的敏感度分析或消融实验。
    - **客观性**：对比策略仅包含两种基线，数量较少；且合成数据中的“学生行为”与真实用户存在差异，可能引入偏差。整体来看，实验更偏向系统演示验证，而非全面、深度的性能评估，充分性一般。

### 6. 主要结论与发现
- 价值感知策略能够通过**选择性保留高价值记忆**，在 LLM 智能体的长期交互中实现**响应质量、检索延迟和 Token 消耗三者间更好的权衡**。
- 基于 Shapley 值的记忆评估方法可以有效识别和量化异构记忆的贡献，使低价值、冗余记忆的剔除变得可解释。
- 交互式分析仪表板能使用户透明地观察记忆的价值分布、层次化结构和存储演变，验证了该系统在**可解释性和交互性**上的优势。

### 7. 优点
- **方法新颖性**：首次将 Shapley 值的公平分配思想引入 LLM 智能体记忆管理，为“记忆价值”提供了原则性的量化手段，并设计了实用的近似算法保证交互式效率。
- **系统完整性**：提供了覆盖评估-存储-响应全流程的端到端框架，并构建了可视化分析仪表板，让黑箱的记忆管理过程透明化、可交互。
- **设计实用**：将记忆组织为层次化树并支持合并更新，有效平衡了存储效率与信息保真度；用户可灵活调整阈值，按需权衡成本与性能。

### 8. 不足与局限
- **实验验证薄弱**：仅在单一自建合成数据集上进行演示级对比，缺乏在真实用户场景和多种公开基准上的广泛评估，结论的泛化性存疑。
- **计算开销未量化**：Shapley 值的近似计算和 LLM 作为评判器的评分，本身会引入推理成本和延迟，文中未对其开销进行详细分析或与现有方法的效率对比。
- **依赖外部评判模型**：记忆价值的评估基于“LLM 评分”，其可靠性取决于评判模型（Judge Model）自身的偏见和准确度，可能将评分误差传播到整个生命周期。
- **动态性处理有限**：记忆价值可能随时间、用户状态变化而漂移，当前框架主要基于历史交互做一次性评估，缺少对价值动态演变的显式建模和更新机制。

（完）
