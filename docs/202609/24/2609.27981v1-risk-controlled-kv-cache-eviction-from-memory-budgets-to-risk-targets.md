---
title: "Risk-Controlled KV-Cache Eviction: From Memory Budgets to Risk Targets"
title_zh: 风险控制的 KV 缓存驱逐：从内存预算到风险目标
authors: "Beomgu Kang, SoJin Yun, Hojoon Kim, Hyunseok Seo"
date: 2026-09-23
pdf: "https://arxiv.org/pdf/2609.27981v1"
tags: ["query:awc"]
score: 8.0
evidence: 将KV缓存驱逐重构为部署风险控制
tldr: "KV-cache驱逐常以平均质量-内存权衡评估，但平均损失小仍可能掩盖单请求效用显著下降。本文将驱逐重构为部署风险控制，以相对全KV推理的任务效用退化频率定义风险，并用压缩器无关的后验认证从校准数据选择保留策略，未认证则回退全KV。实验在Llama/Mistral与LongBench/RULER-32K上表明，同一可靠性合约可认证不同驱逐强度：Llama LongBench可认证SnapKV 75%保留，RULER-32K无压缩策略通过。该框架把部署可靠性要求转化为KV内存操作点。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.27981v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 786, \"height\": 592, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.27981v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1169, \"height\": 890, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.27981v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 785, \"height\": 526, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.27981v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 784, \"height\": 308, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.27981v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 803, \"height\": 425, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.27981v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1659, \"height\": 671, \"label\": \"Table\"}]"
motivation: 传统KV-cache驱逐只看平均质量-内存权衡，平均损失小会掩盖部分请求效用显著退化，无法满足部署可靠性要求。
method: 将驱逐建模为部署风险控制：以相对全KV推理的任务效用退化频率定义风险，用压缩器无关后验认证从校准数据选保留策略，无认证则回退全KV。
result: "在Llama/Mistral与LongBench/RULER-32K上，同一合约认证不同驱逐强度：Llama LongBench认证SnapKV 75%保留，RULER-32K无压缩策略通过而回退全KV；经验阈值会选未认证策略并少留5-10个百分点缓存。"
conclusion: 该框架把部署级可靠性要求转化为KV内存操作点，用有限样本认证控制退化风险，避免平均指标误导。
---

## 摘要
KV 缓存驱逐通常通过平均质量-内存权衡来评估，然而较小的平均损失可能掩盖那些效用显著下降的请求。我们将驱逐重新表述为一个部署风险控制问题：当驱逐相对于同一请求的全 KV 推理使任务效用降低超过部署指定的容差时，就发生了实质性退化，而部署风险是此类事件在总体中的发生频率。给定指定目标风险水平和置信度要求的可靠性契约，我们使用一种与压缩器无关的事后认证程序，从校准数据中选择具有有限样本保证的保留策略；当没有压缩策略通过认证时，则回退到全 KV。在多种驱逐方法、Llama 和 Mistral 模型以及 LongBench 和 RULER-32K 上，同一契约支持显著不同级别的驱逐：在 Llama 上，它在 LongBench 上认证 SnapKV 的 75% 保留率，但在 RULER-32K 上没有认证任何测试过的压缩策略，从而触发全 KV 回退。经验退化率低于 5% 目标的策略仍可能无法通过有限样本认证；在 Llama LongBench 上，经验阈值法选择的未认证策略在固定预算方法中少保留 5-10 个百分点的缓存。所提出的框架将部署级可靠性要求转化为 KV 内存工作点。

## Abstract
KV-cache eviction is typically evaluated through average quality-memory trade-offs, yet a small average loss can hide requests whose utility degrades materially. We reformulate eviction as a deployment risk-control problem: a material degradation occurs when eviction lowers task utility by more than a deployment-specified tolerance relative to full-KV inference on the same request, and deployment risk is the population frequency of such events. Given a reliability contract specifying a target risk level and confidence requirement, we use a compressor-agnostic post-hoc certification procedure to select a retention policy from calibration data with a finite-sample guarantee, falling back to full KV when no compressed policy is certified. Across multiple eviction methods, Llama and Mistral models, and LongBench and RULER-32K, the same contract supports substantially different levels of eviction: on Llama, it certifies SnapKV at 75% retention on LongBench but no tested compressed policy on RULER-32K, triggering full-KV fallback. Policies with empirical degradation rates below the 5% target can still fail finite-sample certification; on Llama LongBench, empirical thresholding selects uncertified policies that retain 5-10 percentage points less cache across fixed-budget methods. The proposed framework converts a deployment-level reliability requirement into a KV-memory operating point.

---

## 论文详细总结（自动生成）

# 论文总结：Risk-Controlled KV-Cache Eviction: From Memory Budgets to Risk Targets

## 1. 核心问题与整体含义
- **研究背景**：长上下文 LLM 推理中，KV cache 随序列长度和并发请求增长而成为主要内存瓶颈，已有大量 KV-cache 驱逐方法试图在有限内存预算下保留重要 KV 状态。
- **核心问题**：现有评估多关注平均质量-内存权衡，但**平均损失小并不代表请求级风险低**。一个很小的平均退化可能掩盖部分请求的显著效用下降。
- **形式化动机**：论文区分平均退化与尾部风险，指出 \(E[\Delta_\pi(X)]\) 小并不能推出 \(P[\Delta_\pi(X)>\tau]\le \epsilon\)。因此需要直接控制“请求效用下降超过容差”的频率。
- **整体含义**：论文将 KV-cache 驱逐从“先定内存预算、再看平均质量”重构为“部署风险控制”问题：给定可靠性合约，从校准数据中选择可认证的保留策略；若无压缩策略可认证，则回退到全 KV 推理。

## 2. 论文提出的方法论
- **核心思想**：将 KV 驱逐策略视为待认证的风险假设，以“相对同一请求全 KV 推理的效用退化”定义请求级风险，并用有限样本统计认证选择策略。
- **风险定义**：
  - 有符号退化：\(\Delta_\pi(X)=U_{\text{full}}(X)-U_\pi(X)\)。
  - 实质性退化事件：\(V_\tau(X;\pi)=\mathbf{1}\{\Delta_\pi(X)>\tau\}\)。
  - 部署风险：\(R_\tau(\pi)=\mathbb{E}_{X\sim P}[V_\tau(X;\pi)]\)。
  - 可靠性合约：\(R_\tau(\pi)\le \epsilon\)，并要求有限样本保证 \(Pr_{CAL}\{R_\tau(\hat\pi)>\epsilon\}\le \delta\)。
- **优化目标**：理想目标是 \(\min_{\pi\in\Pi} b(\pi)\)，使得 \(R_\tau(\pi)\le\epsilon\)，其中 \(b(\pi)\) 为保留缓存比例；但论文不直接求解全局最优，而是从预定义策略序列中做统计认证，并允许全 KV 回退。
- **认证流程**：
  - 基于 Learn-then-Test（LTT）与固定序列测试。
  - 固定预算方法预定义保留率序列，例如 \((0.80,0.75,\dots,0.20)\)，从保守到激进排序。
  - 对候选 \(\pi_j\) 计算 CAL 违规计数 \(K_j=\sum_i V_\tau(X_i;\pi_j)\)，经验风险 \(\hat R_{\tau,j}=K_j/n\)。
  - 检验 \(H_j:R_\tau(\pi_j)\ge\epsilon\) 对 \(H_{Aj}:R_\tau(\pi_j)<\epsilon\)，认证需证据表明风险严格低于目标。
  - 使用二项下尾构造 p 值：若 \(K_j\le \lfloor n\epsilon\rfloor-1\)，则 \(p^{\text{env}}_j=Pr\{\text{Bin}(n,\epsilon)\le K_j\}\)，否则为 1。
  - 若 \(p^{\text{env}}_j\le\delta\)，则认证该候选；按预定义顺序测试，停在首个未认证候选，返回最后一个已认证候选；若首个候选未认证，则返回全 KV。
- **任务分层校准**：目标总体定义为任务均匀混合 \(P=\frac1H\sum_h P_h\)，CAL 中每个任务取 \(n/H\) 个样本。证书针对任务平衡总体风险，而非每个任务单独风险。
- **有限样本严格性**：例如 \(n=1200,\epsilon=0.05,\delta=0.05\) 时，最大可认证违规数为 \(K_{\max}=47\)，对应经验风险 3.92%，比名义 5% 更严格；RULER 中 \(n=1300\) 时 \(K_{\max}=51\)。
- **请求自适应扩展**：同一框架可用于 ReFreeKV 等请求自适应压缩器，其阈值 \(T\) 定义策略 \(\pi_T\)，按预定义阈值序列认证，并估计平均保留比例。

## 3. 实验设计
- **模型**：
  - Llama-3.1-8B-Instruct
  - Mistral-7B-Instruct-v0.3
- **KV 驱逐
