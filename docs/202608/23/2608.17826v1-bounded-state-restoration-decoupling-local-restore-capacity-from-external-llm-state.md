---
title: "Bounded-State Restoration: Decoupling Local Restore Capacity from External LLM State"
title_zh: 有界状态恢复：将本地恢复能力与外部LLM状态解耦
authors: Zixuan Li
date: 2026-08-18
pdf: "https://arxiv.org/pdf/2608.17826v1"
tags: ["query:cache-reuse"]
score: 8.0
evidence: 面向长上下文代理状态的分层KV缓存恢复
tldr: "长上下文LLM的层级KV缓存虽能保留外部状态，但恢复时所需的本地暂存容量（RWS）未被独立考量。本文提出有界状态恢复（BSR），将状态发现与本地物化解耦，以窗口W限制恢复峰值容量，同时保持总传输与安装开销为Θ(|S|)。在DeepSeek-V4-Flash上，外部状态达31.277 GiB/rank时L1 RWS仅500.75 MiB/rank，并支持容错回退与SSD加速恢复。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.17826v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1518, \"height\": 789}, {\"url\": \"assets/figures/arxiv/2608.17826v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1673, \"height\": 795}, {\"url\": \"assets/figures/arxiv/2608.17826v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1676, \"height\": 778}, {\"url\": \"assets/figures/arxiv/2608.17826v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1479, \"height\": 768}, {\"url\": \"assets/figures/arxiv/2608.17826v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1463, \"height\": 793}, {\"url\": \"assets/figures/arxiv/2608.17826v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1144, \"height\": 609}, {\"url\": \"assets/figures/arxiv/2608.17826v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1042, \"height\": 596}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.17826v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1722, \"height\": 903}, {\"url\": \"assets/tables/arxiv/2608.17826v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1659, \"height\": 365}, {\"url\": \"assets/tables/arxiv/2608.17826v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1716, \"height\": 505}, {\"url\": \"assets/tables/arxiv/2608.17826v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1335, \"height\": 269}, {\"url\": \"assets/tables/arxiv/2608.17826v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1379, \"height\": 415}]"
motivation: 层级KV缓存保留状态的能力不等于恢复所需本地内存，需显式隔离并优化恢复工作集RWS。
method: BSR先探测完整可复用前缀，再通过最多W个块的窗口安装确认状态，并采用请求级提交规则处理异构分配器与TP秩故障。
result: 外部状态31.277 GiB/rank时RWS仅500.75 MiB/rank（63.9倍压缩比），恢复TTFT从43.1秒降至17.6秒。
conclusion: BSR实现了恢复容量与外部状态的解耦，为超长上下文恢复提供可扩展、容错的资源边界。
---

## 摘要
分层KV缓存系统可以在GPU内存之外保留长上下文LLM执行状态，但保留容量并不决定使该状态再次可执行所需的本地内存。我们将这第二个资源隔离为恢复工作集（RWS）：在恢复期间生命周期重叠的峰值本地暂存状态。在固定的上游LMCache全计划路径中，1.956、7.823和15.646 GiB/rank状态的实测完全重用点首先在2、8和16 GiB L1档位成功，成功的L1峰值为1.956、7.824和15.648 GiB/rank。我们引入有界状态恢复（BSR），将完整发现与本地驻留分离。BSR在不将整个命中物化到L1的情况下探测完整的可重用前缀，然后通过最多W个块的可重用窗口安装确认的状态。在有界辅助状态下，峰值恢复容量为O(W)，而总传输和安装工作保持Θ(|S|)。由于可重用状态跨越异构分配器组和张量并行秩，BSR使用请求级提交规则：部分安装从不作为有效的可重用前缀暴露；失败会使所公告的前缀失效，并回退到较低的有效层或确定性重计算。在TP=2的两个DGX Spark节点上的DeepSeek-V4-Flash上，一次干净的未恢复扫描将外部状态从1.956 GiB/rank增长到31.277 GiB/rank，而测量的L1 RWS在W=32时正好保持500.75 MiB/rank，最大状态外部到实时暂存比为63.959倍。第二次全新的524K token运行重复了最大状态接受结果。评估的层和秩不对称失败在回退之前暴露了完全重用或零外部重用。匹配的SSD优化将512K恢复TTFT从43.1秒减少到17.6秒，而不改变RWS。

## Abstract
Hierarchical KV-cache systems can retain long-context LLM execution state beyond GPU memory, but retention capacity does not determine the local memory required to make that state executable again. We isolate this second resource as the restoration working set (RWS): the peak local staging state whose lifetimes overlap during restoration. In the pinned upstream LMCache whole-plan path, measured full-reuse points for 1.956, 7.823, and 15.646 GiB/rank states first succeed at 2, 8, and 16 GiB L1 rungs, with successful L1 peaks of 1.956, 7.824, and 15.648 GiB/rank.   We introduce Bounded-State Restoration (BSR), which separates complete discovery from local residency. BSR probes the complete reusable prefix without materializing the whole hit in L1, then installs confirmed state through a reusable window of at most $W$ chunks. Under bounded auxiliary state, peak restoration capacity is $O(W)$ while total transfer and installation work remains $Θ(|S|)$. Because reusable state spans heterogeneous allocator groups and tensor-parallel ranks, BSR uses a request-level commit rule: partial installation is never exposed as a valid reusable prefix; failures invalidate the advertised prefix and fall back to a lower valid tier or deterministic recomputation.   On DeepSeek-V4-Flash with TP=2 across two DGX Spark nodes, a clean no-resume sweep grows external state from 1.956 to 31.277 GiB/rank while measured L1 RWS remains exactly 500.75 MiB/rank at $W=32$, a 63.959x largest-state external-to-live-staging ratio. A second fresh 524K-token run repeats the largest-state acceptance result. Evaluated tier and rank-asymmetric failures expose either complete reuse or zero external reuse before fallback. A matched SSD optimization reduces 512K restore TTFT from 43.1 to 17.6 seconds without changing RWS.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：长上下文 LLM 服务越来越依赖分层 / 分离式 KV 缓存系统，将先前计算的可复用执行状态（KV cache）保存在宿主机内存、SSD 或远程内存中，以避免重复 prefill。这类系统主要扩展的是**外部保留容量**，即状态可以存在多远、多大。
- **核心问题**：论文指出，**保留容量（retention capacity）与恢复所需的本地容量（restoration capacity）是两个完全不同的资源**。外部存储能保留巨大的状态，但恢复路径上本地暂存区（staging）可能需要与完整选中状态同量级的空间，高层的缓存命中并不等于本地能够把它重新变为可执行状态。
- **核心概念**：论文将恢复过程中同时存活的本地暂存状态的峰值定义为**恢复工作集（Restoration Working Set, RWS）**，并明确提出一个问题：能否做到“完整知道可复用外部状态，却只通过显式有界的小本地工作集来恢复，同时保持请求级正确性和失败回退安全”？
- **总体意义**：这是第一项明确区分“外部保留容量”与“本地恢复容量”并针对后者提出协议级方案的工作，为分层 KV 缓存系统的容量规划与资源编排提供新维度。

### 2. 论文提出的方法论：核心思想、关键技术细节与公式

- **核心思想**：**完全的知识并不要求完全的本地驻留**（complete knowledge does not require complete local residency）。BSR 将“发现完整可复用前缀”与“在本地物化状态”分离：先探测完整命中集合（不物化全部字节），再通过一个最多包含 W 个块的可重用窗口迭代安装确认的状态。因此，**更大的外部状态只增加迭代次数，不增加同时存活的本地暂存量**。
- **关键定义与公式**：
  - 恢复工作集（RWS）形式化定义：RWS(ρ) = maxₜ Σ_{uᵢ ∈ L_ρ(t)} cᵢ，其中 L_ρ(t) 是时刻 t 同时在本地暂存层存活的恢复单元集合，cᵢ 是单元大小。
  - 全计划物化的容量需求：RWS_whole(S) ≥ Σᵢ cᵢ = Θ(|S|)。
  - BSR 的存活不变式：|L_ρ(t)| ≤ W 在受限安装全程成立。
  - 峰值容量上界：RWS_BSR(S, W) ≤ W·c_max + M_aux(W)，即 O(W)，与状态大小 N 无关（命题 1，capacity decoupling）。
  - 总量不随空间优化而减少：总传输和安装工作仍为 Θ(|S|)，延迟随 |S| 增长，BSR 是空间抽象而非时间常量保证。
  - 对该注册，窗口容量定律：ML1(W) = W × 16,408,576 bytes/rank。
- **协议三阶段**：
  1. **探测（Probe globally）**：查询存储层确定保留的命中集但不将完整结果驻留 L1，释放查找端锁，输出命中位图（知识而非驻留）。
  2. **安装（Install locally）**：分配目标块后，以 ≤W 个块为单位迭代窗口：取回 → 验证完整 → 映射到目标块 → 传输 → 同步 → 释放 → 复用暂存。
  3. **提交（Commit at request semantics）**：只有所有 TP 秩 r 和语义组 g 的状态都有效（Q(S) = ∧_r ∧_g q_{r,g} = 1）才暴露完整前缀复用；任何失败则使公告前缀失效，回退到较低有效层或确定性重计算（命题 2，fail-closed）。
- **实现机制**：扩展 LMCache 和 vLLM，新增 probe-only 查找、窗口化取回、有界取回锁、失败块失效、HMA 感知全前缀失效、force-local 恢复标记、FS 加载并发等，不修改模型内核与外部状态表示。

### 3. 实验设计：数据集 / 场景、Benchmark、对比方法

- **模型与场景**：DeepSeek-V4-Flash（MTP-off 注册 167 个张量，MTP-on 注册 170 个张量），TP=2，跨两个 DGX Spark 节点；上下文长度到 524,032 tokens。
- **分层存储层次**：GPU 目标状态 ← LMCache L1 有界暂存窗口 ← Mooncake 远程 DRAM（100 Gb/s RDMA 直连）与本地 SSD/NVMe（较低可复用层），另含确定性重计算作为最终回退。
- **基准对比**：以上游固定 LMCache 全计划（whole-plan）恢复路径作为基线（W=0，保留旧行为），对比 BSR 的 W=32 等有界窗口路径。
- **主要实验组**：
  - **实验1（上游容量阶梯）**：32K/128K/256K 前缀分别在 2 / 8 / 16 GiB L1 档位首次全复用成功，测量峰值 L1 用量。
  - **实验2（干净同 epoch BSR 扫描）**：一个无恢复、不重启引擎的扫描，W=32，6 个前缀尺寸（32K 至 524K），外部状态从 1.956 增至 31.277 GiB/rank。
  - **辅助实验**：W ∈ {8, 16, 32, 64} 的容量定律验证、MTP-on 524K 异构状态确认、失败矩阵（远程不可用、远程+SSD 均缺席、单秩远程故障）、SSD 并发 A/B（并发 1 vs 4）。
- **接受标准**：命中缓存 token 数正确、分层计数器符合预期、输出 hash 与冷执行一致、注册状态几何匹配、双秩健康。

### 4. 资源与算力

- **硬件**：论文未明确 GPU 型号与数量；仅说明使用**两个 DGX Spark 节点，TP=2**，每个 DGX 秩有一条 100 Gb/s 直连 RDMA 到外部 Mooncake store（128 GiB 分段）。Mooncake 单秩 4 GiB 测量达 10.31/10.34 GB/s GET 和 10.83 GB/s PUT；双秩并发合计 18.87 GB/s GET、21.21 GB/s PUT。
- **训练 / 运行时长**：论文未涉及训练；推理侧未给出明确的单次实验运行时长或总 GPU 时数，仅报告了恢复 TTFT（如 512K SSD 43.1→17.6 秒；Mooncake 路径 32K→524K 为 2.17→16.41 秒）。
- **总体评价**：资源信息披露不完整，未说明 GPU 型号、数量、功耗或总实验时长。

### 5. 实验数量与充分性

- **实验数量**：
  - 全计划阶梯：3 个有效 PASS 点（32K/128K/256K），外加 1 次被取代的历史 256K 尝试、1 个未完成的 512K 全计划测量。
  - BSR 干净扫描：6 个前缀尺寸的固定-W 主结果 + 1 次全新的 524K 复现运行。
  - 窗口定律：4 组 W（8 / 16 / 32 / 64）各一次 256K 测量。
  - 异构 MTP-on：1 组 524K 测量。
  - 失败矩阵：4 种失败条件各一次。
  - SSD A/B：2 组（并发 1 vs 4）。
- **充分性评估**：
  - **优点**：主结论（外部状态增长 15.99 倍而 RWS 完全不变）基于单次干净、无恢复、不重启引擎的同 epoch 扫描，排除了跨 epoch 干扰；测量 L1 峰值范围为零字节，数据非常整洁。复现运行与接受标准都较强。
  - **不足**：整体实验数量较少，每个条件基本只有一次有效运行；无多次重复的分布 / 方差报告；窗口定律的非单调 TTFT 被明确声明为描述性数据而非延迟结论；失败矩阵每组仅一种场景；无多请求并发压力测试（当前串行锁限制）；512K 全计划上游点未完成，对比图省略该点。因此，结论的内部效度较强，但外部推广性（跨模型、跨硬件、多并发）有限。

### 6. 论文的主要结论与发现

- 上游全计划路径中，恢复峰值本地容量与所选外部状态规模呈 Θ(|S|) 正相关：1.956 / 7.823 / 15.646 GiB/rank 的状态分别需要 2 / 8 / 16 GiB L1 档位，成功峰值分别为 1.956 / 7.824 / 15.648 GiB/rank。
- BSR 在 W=32 时，外部状态从 1.956 增长至 31.277 GiB/rank（15.99 倍），而 L1 RWS 保持 500.75 MiB/rank 不变，实测范围为 0 字节，最大状态与实时暂存比为 63.959×。
- W 是显式且可预测的容量控制：W ∈ {8, 16, 32, 64} 时实测 RWS 精确等于 W × 16,408,576 bytes/rank，零残差。
- 异构（MTP-on、170 张量）与故障场景下，失败回退始终暴露“完整前缀复用”或“零外部复用”两种极端，没有部分无效前缀被暴露，满足 fail-closed 要求。
- 恢复吞吐与恢复容量可独立控制：仅将 SSD 对象加载并发从 1 提到 4，512K 恢复 TTFT 从 43.1 秒降到 17.6 秒（2.45×），RWS 不变。
- 总体结论：外部容量、恢复容量、执行余量和恢复吞吐是四个不同资源，BSR 证明了可以在保留大外部状态的同时以小而有界的本地暂存实现可靠的请求级恢复。

### 7. 优点

- **概念清晰、定义严谨**：RWS 的定义（式 1）明确将恢复暂存与外部保留和正常执行内存区分开，解决了领域内一个被忽视的资源维度。
- **理论驱动的协议设计**：完整知识 / 本地驻留分离 + 窗口迭代安装 + 请求级提交三阶段协议有清晰的复杂度结论（O(W) 空间、Θ(|S|) 总工作），并有命题支撑。
- **实验设计严谨**：以单一干净扫锚定主容量结论，明确排除中断尝试，可复现的 evidence 快照与接受契约（输出 hash + 分层计数器 + 双秩健康 + 注册几何）都远超简单的字节数核对；第二次新鲜运行验证了结论的鲁棒性。
- **失败安全语义完整**：HMA 感知的全前缀失效 + force-local 标记 + 零外部复用回退，覆盖了异构状态与秩故障场景，部分安装不会被误用。
- **实现面窄、可落地**：不修改模型内核和外部状态表示，仅扩展 LMCache/vLLM 的控制路径，工程侵入性低。
- **容量与吞吐正交性验证**：SSD A/B 证明在不改变空间契约的情况下可以独立优化吞吐，结论有实际部署价值。

### 8. 不足与局限

- **实验规模有限**：单模型（DeepSeek-V4-Flash）、单注册拓扑、TP=2 两个节点；无跨模型、跨硬件、更大规模（如 TP 更多、多机集群）验证，外部有效性受限。
- **并行度保守**：原型用全局锁串行化有界恢复，无多请求并发测试，实际部署中可能成为吞吐瓶颈；论文将此列为自然扩展方向而非已验证能力。
- **正确性验证是操作性而非形式的**：接受标准虽强，但不是对每个隐藏状态等价的正式证明；命题 2 依赖机制按实现的假设，未做形式验证。
- **某些数据点缺失或非测量值**：512K 全计划上游点未完成，图 4 中无该柱；512K 全计划对应的 32 GiB 容量需求为推算而非实测。
- **窗口定律的普适性未知**：16,408,576 字节/W 是特定模型注册独有的系数，其他模型 / 状态表示会有不同常数，论文也明确此系数不通用。
- **延迟数据是描述性的**：窗口 W 的 TTFT 非单调且仅单次运行，未做重复随机化实验；论文明确不据此得出万能最优 W 的结论。
- **RWS 不是整个进程内存**：论文只约束 LMCache L1 恢复池这一最强调不变式，模型权重、目标 KV、分配器元数据等仍可能变化；对整体进程 UMA 的节省未做宣称。
- **AI 辅助披露**：论文明确使用 LLM 作为执行代理和研究辅助，需要读者注意作者最终负责核验的事实前提。

---

（完）
